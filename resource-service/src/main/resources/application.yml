server:
  port: 8080
spring:
  datasource:
    url: jdbc:postgresql://resource-service-db:5432/resources
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
